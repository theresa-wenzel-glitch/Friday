# Datenbank: SQLite (Standard, lokal). Fuer Produktion siehe README (Postgres).
DATABASE_URL="file:./dev.db"

# Geheimer Schluessel fuer die Login-Sessions. Mit `openssl rand -base64 32` erzeugen.
AUTH_SECRET="bitte-aendern-mindestens-32-zeichen-langer-zufallswert"
