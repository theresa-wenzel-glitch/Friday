{
  "name": "@jobflow/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Design-Tokens und geteilte Konstanten. Zentral definiert, damit sich das Branding an einer Stelle aendern laesst.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "react-native": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
